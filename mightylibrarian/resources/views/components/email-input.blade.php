<div class="form-group">
    <label for="{{$id}}">{{$label}}</label>
    <input type="email"
           class="form-control rounded-3"
           id="{{$id}}"
           name="{{$name}}"
           placeholder="{{$placeholder}}"
           value="{{$value}}">
</div>
